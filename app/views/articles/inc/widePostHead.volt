<section style="background-image: url({{ article.getBigImagePath() }});" class="smile">
	<div class="container smile__container">
		<div class="smile__title">{{ article.categoryName }}</div>
		<h1 class="smile__desc">{{ article.post_title }}</h1>
	</div>
</section>
